const MovieReviewsApp = function(reviews, movies) {
  let tweets = [];

  // Put your code here !

  return {
    getTweets: function() {
      return tweets;
    }
  };
};

module.exports = MovieReviewsApp;
